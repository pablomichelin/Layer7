# Histórico — Fecho PORTAL-PLAN-004 (`2.0.0`)

| Campo | Valor |
|-------|-------|
| Plano | PORTAL-PLAN-004 |
| Versão | `2.0.0` |
| Data | `2026-08-08` |

## Entrega

- Schema RBAC em `admins` (`is_owner`, `is_active`, `permissions`)
- Módulo `admin-permissions` + middleware `requirePermission`
- API `/api/users` (CRUD técnicos; owners só via bootstrap)
- UI Utilizadores + gates no menu e acções
- Deploy live com rsync `--exclude .env`

## Rollback

Imagens Docker anteriores; colunas aditivas.
