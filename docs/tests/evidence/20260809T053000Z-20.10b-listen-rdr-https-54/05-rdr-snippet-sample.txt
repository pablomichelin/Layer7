# Sample generated by layer7_generate_mitm_rdr_snippet when mitm_effective
# and intercept.dest_cidr non-empty (PHP unit test PASS on builder).
table <layer7_mitm_dst> persist { 203.0.113.10 }
rdr on lan inet proto tcp from any to <layer7_mitm_dst> port 443 -> 127.0.0.1 port 8443
# Note: builder FreeBSD 15 lacks pf netlink for pfctl -nf; form matches blockpage rdr.
