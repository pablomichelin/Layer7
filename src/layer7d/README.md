# layer7d (fonte)

Daemon C (Bloco 6). **Empacotamento:** `package/pfSense-pkg-layer7` compila este `main.c` em `make package`.

Compilação manual:

```sh
cc -Wall -Wextra -O2 -o layer7d main.c
./layer7d -h
```
