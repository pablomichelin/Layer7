# Editor / OS
Thumbs.db
Desktop.ini
*.swp
*.swo
.vscode/
.idea/

# Cursor / local
.cursor/

# Inventário do lab preenchido localmente (não commitar IPs/senhas)
docs/08-lab/lab-inventory.md

# Build artifacts (futuro) — só na raiz (não ignorar scripts/build/)
*.txz
*.pkg
/dist/
/build/

# Binários locais (make / smoke em src/layer7d)
src/layer7d/layer7d
src/layer7d/layer7d-smoke
